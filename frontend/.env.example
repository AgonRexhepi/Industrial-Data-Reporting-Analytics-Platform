# URL of the backend API. When using the Vite dev server, leave this unset
# and the built-in proxy will forward /api requests to http://localhost:8000.
# VITE_API_BASE_URL=http://localhost:8000/api/v1
